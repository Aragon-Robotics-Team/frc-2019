package frc.robot.commands.rgb;

import edu.wpi.first.wpilibj.command.CommandGroup;

public class RGBAutonomous extends CommandGroup {
    public RGBAutonomous() {
        // addParallel(new HueCycle(2.0));
        // addParallel(new StrobeSquareWave(1.0 / 10.0, 0.2));
        // addParallel(new TriangleWave(60.0 / 125.0));
    }
}
